export enum Gender {
  MALE = 1,
  FEMALE = 2,
  OTHER = 3
}

export enum UserStatus {
  ACTIVE = 1,
  DELETED = 2
}

export enum Difficulty {
  EASY = 1,
  MEDIUM = 2,
  HARD = 3
}



export enum TopicType {
  TOPIC_TYPE_LESSON = 1,
  TOPIC_TYPE_EXERCISE = 2,
  TOPIC_TYPE_TEST = 3
}

export enum SubmitType {
  CHECK_ON_ANSWER = 1,
  CHECK_ON_SUBMIT = 2
}

export enum CardGame {
  QUIZ = 1,
  SPELLING = 2,
  /** Game đoạn văn */
  PARA = 3
}

export enum Status {
  ACTIVE = 1,
  INACTIVE = 2
}

export enum MetaRobot {
  INDEX = 1,
  NOINDEX = 2,
  FOLLOW = 3,
  NOFOLLOW = 4
}
